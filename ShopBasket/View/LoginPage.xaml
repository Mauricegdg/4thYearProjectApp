<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ShopBasket.View.LoginPage">
    <ContentPage.Content>
        <ScrollView>
            <Grid VerticalOptions="CenterAndExpand">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Entry Placeholder="UserName" x:Name="EntryUserName" Grid.Row="0"/>
                <Entry Placeholder="Password" IsPassword="True" x:Name="EntryPassword" Grid.Row="1"/>
                <Button Text="Login" Clicked="Button_Clicked_1" BackgroundColor="Blue" TextColor="White" FontSize="16"  Grid.Row="2"/>
                <Button Text="SignUp" Grid.Row="3" Clicked="Button_Clicked"/>
            </Grid>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>